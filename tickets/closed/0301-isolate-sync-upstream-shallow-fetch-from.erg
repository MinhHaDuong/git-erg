%erg 0.1
Title: Isolate sync upstream shallow fetch from adopter repository
Created: 2026-09-17
Author: Codex
Closed: fixed: isolate shallow upstream fetch and preserve adopter git state

--- log ---
2026-09-17T08:51Z Codex created

2026-09-17T09:03Z Codex closed — fixed: isolate shallow upstream fetch and preserve adopter git state
2026-09-17T11:05Z haduong note ticked the exit criteria after the #371 post-merge review; its "Linux x86-64 traveler" wording nit from #370 (canRunTravelingBinary hardcodes linux/amd64 and the message names that platform whatever the fetched bytes are) is deferred: sniff the fetched bytes, no ticket until a non-Linux adopter with a vendored native erg exists
--- body ---

## Context

Fable's post-merge review of PR #370 measured that `erg sync --upstream`
runs `git fetch --depth=1` inside the adopter repository. Git consequently
leaves a durable `.git/shallow` graft there. The test proves that old upstream
history is absent but cannot see this cost.

The same review found that a missing blob at the trusted project origin now
fails a chain that is intended to remain offline-tolerant.

Review: https://github.com/MinhHaDuong/git-erg/pull/370#pullrequestreview-5233295500

## Exit criteria

- [x] `sync --upstream` fetches shallowly in a throwaway repository confined
      to the ticket store and leaves the adopter repository non-shallow without
      importing upstream history
- [x] focused integration coverage proves both halves of that property
- [x] a reachable trusted project origin missing its store-relative binary
      warns but exits 0; explicit custom/upstream sources still fail hard
- [x] help text describes where each fetch runs
- [x] `make test`, `make validate`, and the ticket DAG pass
- [x] external verification has no unresolved findings -- the post-merge
      review of #371 confirmed criteria 1-5 and found two Medium, three Low
      and two Nit follow-ups, all carried by 0302

## Review disposition

Fable also suggested hand-adding the predecessor `.ergrc` hash. That is not
part of this fix: 0281 already measured zero beneficiaries, closed the table
as wontfix, and records that it must never be hand-typed because a false hash
can silently clobber a local edit. The existing preserve + exit 2 degradation
is deliberately safer until empirical need justifies the generated-history
design specified there.
