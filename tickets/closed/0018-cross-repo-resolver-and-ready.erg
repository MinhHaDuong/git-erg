%erg 0.1
Title: Cross-repo resolver, cache, and erg ready integration
Created: 2026-05-02
Author: claude
Closed: abandoned — remote resolution dropped; forge refs are permanently unknown; superseded by 0030
Blocked-by: 0016
Blocked-by: 0017

--- log ---
2026-05-02T01:06Z claude created — 0016 split, child 2 of 3 (network resolver, cache, ready policy)
2026-05-03T00:00Z claude revised — offline is the default; resolver is opt-in via --resolve flag
2026-05-03T00:00Z claude revised — drop on-disk cache; every --resolve call hits the forge API fresh
2026-05-03T00:00Z claude closed — erg contract is offline-first in isolated VMs; remote refs are permanently unknown; no resolver ever; grammar change split to 0030

--- body ---
## Design constraint

erg must work offline in isolated VMs. Network resolution is opt-in,
not the default. Cross-repo refs are `Unknown` by default; `--resolve`
triggers GitHub API calls.

## Scope

The opt-in network half. Implements the resolver, its short-TTL cache,
the auth chain, and integrates the result into `erg ready`.

In:
- Resolver: takes parsed refs from 0017, returns three-state result.
- On-disk ephemeral cache.
- Auth chain.
- `erg ready` policy for `unknown`.
- `--resolve` flag to opt into network resolution.

Out:
- Spec/grammar/validator. (0017.)
- Graph rendering. (0019, which consumes this resolver.)
- `--offline` flag — offline is the default; no flag needed.
- On-disk cache — dropped; every --resolve call hits the forge API fresh.

## Resolver API

```go
type Resolution struct {
    State  State   // Open | Closed | Unknown
    Reason string  // populated when State == Unknown
}

type State int
const (
    Open State = iota
    Closed
    Unknown
)
```

`Closed` for issues means `state == "closed"`. `Closed` for PRs
requires `merged_at != null` (resolves Deferred #4 from 0016).
A PR closed-without-merge is `Open` from a blocker standpoint —
the work isn't done.

`Unknown` reasons (free-form string, kept for diagnostics):
`not-found`, `offline`, `rate-limited`, `auth-failed`, `transferred`,
`network-error`.

The resolver does not decide policy. Callers do.

## Default behavior (no --resolve)

All cross-repo refs return `Unknown{offline}` immediately — no network
call. This is correct and expected in isolated VMs.

## Auth chain (resolves Deferred #3; active only with --resolve)

In order, first that yields a token wins:
1. `gh auth token` (if `gh` is on PATH and authenticated).
2. `GITHUB_TOKEN` env var.
3. Unauthenticated (60 req/hr ceiling).

Document the chain in `rules/tickets.md`.

## Issue transfers (resolves Deferred #6; active only with --resolve)

GitHub returns 301 with a new location for transferred issues.
Follow once, log a stderr warning naming the old and new refs.

## `erg ready` integration

Policy: `Unknown` is treated as **blocking** (fail-closed). A
blocker we can't verify is conservatively assumed to still block.
This is also the correct behavior offline — unresolved cross-repo
refs do not falsely unblock a ticket.

Override with `--assume-unknown=open` for interactive use where the
user wants to see the row anyway.

`erg ready` plain-text output appends a state hint when blockers
are non-trivial:

```
0021 ship feature X    (blocked: gh:anthropics/claude-code#1234 [unknown])
```

JSON output gains a `blocked_by` array per row with resolved state
(structure shared with 0019).

## Tests

- Default (no --resolve): all cross-repo refs return Unknown{offline};
  no network calls made.
- Resolver (--resolve): 200/closed → Closed; 200/open → Open; 404 →
  Unknown not-found; 403 rate-limit → Unknown rate-limited; offline →
  Unknown offline; PR open → Open; PR closed-not-merged → Open;
  PR merged → Closed.
- `erg ready`: unknown blocks by default; `--assume-unknown=open`
  surfaces the row.
- Auth chain: each tier picked in order; missing all = unauth call.

## Exit criteria

- [ ] Default mode: cross-repo refs resolve to Unknown{offline} with
      no network calls.
- [ ] `--resolve` flag enables GitHub API resolution.
- [ ] Resolver implementation with three-state result.
- [ ] Auth chain (`gh` → env → unauth) documented and tested.
- [ ] PR-merged distinction enforced (Deferred #4).
- [ ] Issue-transfer 301 followed with warning (Deferred #6).
- [ ] `erg ready` fail-closed on unknown by default;
      `--assume-unknown` flag shipped.
- [ ] `rules/tickets.md` documents auth chain and cache location.
