# Fang Audit — `git-erg` Go test harness

_Mutation-testing audit: each test was probed by breaking the code it covers and checking whether the test goes red. Generated by the `fang-audit` fan-out workflow. No source or test files were changed in the repo — all mutations lived and died in throwaway worktrees._

## Canary (validity gate)

✅ **PASS** — every designated guard test caught its primary canary mutation, so the mutation harness itself works and the findings below are trustworthy:
- `scaling_test.go` → **caught** — In erg.go loadErgs WalkDir callback, wrapped the per-file parseErg in a `for range pairs` nested loop so each file is re-parsed once per already-seen ticket -> total O(N^2) parses and O(N^2) allocated bytes.
- `resource_test.go` → **caught** — atomicwrite.go:88 -- replaced `f.Close()` with `_ = f` after the O_WRONLY writability probe `os.OpenFile(path, os.O_WRONLY, 0)`, leaking one fd per writeTicketAtomic call.

## Summary

| Verdict | Count |
|---|---|
| 🦷 **toothless** (`survived` — mutation went undetected by the file's own tests) | **3** |
| &nbsp;&nbsp;↳ of which ⚠️ **true coverage gap** (no test *anywhere* caught it) | 3 |
| &nbsp;&nbsp;↳ of which caught only by a *sibling* file (this file lacks the fang) | 0 |
| ✅ fang confirmed (`caught` by a same-file test) | 86 |
| 🔵 inconclusive (`equivalent` — no distinguishing input exists) | 3 |
| 🔧 compile-error (mutation skipped) | 0 |
| **files audited** | 19 / 19 |

_Counts are mutations probed, not tests. The 🦷 toothless rows are the actionable headline; the two sub-rows partition them. `caught` is the healthy majority._

## 🦷 Toothless tests (survived a real, distinguishing mutation) — sorted by risk × churn

| Test file | Test func | risk×churn | Mutation | Distinguishing input (why it's wrong) | Suggested fang |
|---|---|---|---|---|---|
| `contract_test.go` | `TestLinearOpCount` | 5.0 (r1×c5) | validate.go validateCorpus rule 10: replaced the O(1) `allIDs[ref.ID]` map lookup with an inner `for k := range tickets { if tickets[k].FilenameID() == ref.ID ... }` linear scan, making per-ref resolution O(N) -> total O(N*refs), i.e. quadratic on a chained corpus. | Distinguishing input: makeCorpusWithRefs at n=50 vs n=100 (the existing fixture). With the mutation, actual comparisons in rule 10 are sum_{i}(refs_i * n) ~ 49*50 vs 99*100, a ~4x growth (quadratic) that should trip the ratio<2.5 ceiling IF it were counted. It is not counted: corpusOpCount is incremented exactly once per ref (validate.go:156, OUTSIDE the new inner loop), so the recorded op-count stays linear (~2.0 ratio, floor still met) while the real CPU work is quadratic. Expected (a quadratic guard): ratio ~4.0 -> FAIL. Actual: ratio ~2.0 -> PASS. The test file's own comment (lines 114-117, 159-163) concedes this: it states the op-counter 'tracks per-ref and per-edge work' and that 'the wall-clock backstop in test_contract.sh covers algorithmic regression inside individual lookups' -- i.e. this exact defect class is deliberately delegated to a shell-level timing test OUTSIDE the Go default suite, so within contract_test.go it is genuinely toothless. | Either (a) add a Go-level wall-clock/allocation backstop in TestLinearOpCount for rule-10 resolution: time cmdCheck on n vs 2n chained corpora and assert the time ratio stays below ~3.0 (mark @integration since it uses timing), or (b) increment a SEPARATE counter inside the actual comparison/lookup of rule 10 (per-comparison, not per-ref) and assert IT scales linearly. Option (b) is deterministic and keeps the guard in the default suite; the current per-ref counter is blind to any work happening inside a single ref's resolution. |
| `check_test.go` | `TestHeaderBlankWarnings` | 4.0 (r1×c4) | In headerBlankWarnings (check.go:136), drop the suffix filter entirely: `if err != nil \|\| d.IsDir() \|\| !strings.HasSuffix(path, ".erg")` -> `if err != nil \|\| d.IsDir()`, so every file under dir is read and tested for an interior header blank. | Distinguishing input within the function's domain: create dir with a single file `notes.txt` (NOT .erg) whose contents are an erg-shaped header with an interior blank, e.g. "%erg 0.1\nTitle: T\nCreated: 2024-01-01\n\nAuthor: a\n\n--- log ---\n--- body ---\n". Pristine: headerBlankWarnings(dir) == nil (file skipped by suffix filter). Mutant: returns ["WARN notes.txt: blank line inside header block -- run `erg migrate` to normalise"]. Expected nil, actual one spurious warning -> observably wrong, so the mutation is non-equivalent and the test is toothless on the scoping invariant. | Add a subtest 'non-.erg files are ignored': write a *.txt (or README.md) into dir containing an interior-header-blank erg-shaped body, plus optionally one real *.erg blank file, and assert that only the .erg file is reported (len(w)==1 and w[0] names the .erg file; no warning mentions the .txt). Red-control: confirm this subtest fails when the .erg suffix filter is removed. |
| `new_test.go` | `TestSlugify` | 4.0 (r1×c4) | new.go:22 boundary `if len(s) > 40` -> `if len(s) > 41` | The only long-title case in TestSlugify slugifies to 65 chars (verified: 'this-is-a-very-long-title-that-exceeds-forty-characters-definitely'), far above both 40 and 41, so the truncate branch fires identically under `> 40` and `> 41`. The test never exercises a slug whose length is exactly 41, which is the single input that distinguishes the two predicates. Distinguishing input: a title slugifying to exactly 41 chars (e.g. 41 'a's) -> pristine `> 40` returns a 40-char slug; mutant `> 41` returns the full 41-char slug. The boundary is asserted only with a value deep inside the truncation region, not at the off-by-one edge, so the test is toothless against a +/-1 boundary error. | Add a boundary case: a title that slugifies to exactly 41 characters and assert the result is exactly 40 chars (e.g. {strings.Repeat("a", 41), strings.Repeat("a", 40)}), plus a 40-char case asserting it is left untouched. This pins both sides of the `> 40` boundary and would catch `> 41`, `>= 40`, and `> 39` mutants. |

_Each row survived both the Opus audit and the Sonnet adversarial skeptic. "Distinguishing input" is a concrete case where the mutated code is observably wrong yet no same-file test failed. Sorted by risk × churn so the highest-blast-radius × change-frequency gaps surface first; risk is the human-supplied weight (default 1)._

### ⚠️ Coverage gaps (no test anywhere caught the mutation)

- `check_test.go` / `TestHeaderBlankWarnings` — The scan is scoped to .erg files only (`strings.HasSuffix(path, ".erg")`); non-ticket files must be ignored so a corpus warning pass never warns on unrelated documents/notes.: In headerBlankWarnings (check.go:136), drop the suffix filter entirely: `if err != nil \|\| d.IsDir() \|\| !strings.HasSuffix(path, ".erg")` -> `if err != nil \|\| d.IsDir()`, so every file under dir is read and tested for an interior header blank.
- `contract_test.go` / `TestLinearOpCount` — algorithmic-regression resistance: the linear-op-count guard claims to defend against superlinear work growing inside corpus reference resolution (rule 10 Blocked-by lookup).: validate.go validateCorpus rule 10: replaced the O(1) `allIDs[ref.ID]` map lookup with an inner `for k := range tickets { if tickets[k].FilenameID() == ref.ID ... }` linear scan, making per-ref resolution O(N) -> total O(N*refs), i.e. quadratic on a chained corpus.
- `new_test.go` / `TestSlugify` — slugify truncates slugs longer than 40 chars to 40: new.go:22 boundary `if len(s) > 40` -> `if len(s) > 41`

## 🔵 Inconclusive (equivalent mutants — NOT accusations)

| Test file | Test func | Mutation | Why behavior-neutral |
|---|---|---|---|
| `list_test.go` | `TestIsDirArg` | list.go: isDirArg drops the 'arg == ".."' literal shortcut, leaving only 'strings.Contains(arg, "/") \|\| arg == "."' before the os.Stat fallback | Within this test's domain the mutated code is observably wrong only in the sense that the dedicated '..' fast-path is gone; the test still passes because go test runs from src/go, so os.Stat("..") stats the real, existing parent directory (src/) and the fallback 'err == nil && info.IsDir()' returns true. Input '..' => mutated returns true via os.Stat fallback; expected true via the literal branch. The literal '..' branch is therefore redundant with the existence check whenever the parent exists (always, in any normal cwd), so the test gives the branch NO fang. A distinguishing input that exposes the loss exists in principle: a process whose cwd has been unlinked so that the parent is not stattable would make pristine return true (literal branch) but mutant return false — outside what this test exercises. |
| `resource_test.go` | `TestScalingFDHygiene_NegativeControl` | None applied to source -- this is a self-contained falsifiable control (os.Open(os.DevNull) held across openFDCount, closed before return). Source mutations cannot trip it because it leaks its own resource. Verified live at baseline. | Not an accusation: a negative control is a fixed self-test, not a source-coupled assertion. There is no source mutation that can distinguish its behavior because it manufactures its own resource leak; its non-vacuity is established by it passing at baseline while asserting a count rise. The matching guard (finding 1/2) is proven caught independently, so the control's job -- detecting a blinded openFDCount helper -- is well-posed and currently live. |
| `resource_test.go` | `TestScalingHeapRetention_NegativeControl` | None applied to source -- self-contained falsifiable control (retains ~1.6 MiB via a [][]byte sink + runtime.KeepAlive, asserts liveHeapBytes climbs past the same heapRetentionSlackBytes the real guard uses). Verified live at baseline. | Not an accusation: like the fd control, this is a fixed self-test decoupled from the mapped source files; no source mutation can distinguish it. Its non-vacuity is demonstrated by passing at baseline while asserting a >slack rise, and the matching guard is independently proven caught (finding 3). |

## ✅ Confirmed fangs (caught by a same-file test)

<details><summary>86 confirmed — expand</summary>

| Test file | Test func | Mutation | Caught by |
|---|---|---|---|
| `atomicwrite_test.go` | `TestAtomicWriteFileRefusesReadOnlyTarget` | Wrapped the O_WRONLY writability probe at atomicwrite.go:87-91 in `if false { ... }`, so the probe never runs and a read-only (0444) target is silently replaced. | TestAtomicWriteFileRefusesReadOnlyTarget |
| `atomicwrite_test.go` | `TestWriteTicketAtomicPreservesExistingMode` | Removed the os.Stat-derived perm at atomicwrite.go:174-177, hardcoding perm := 0644 so the new inode always lands 0644. | TestWriteTicketAtomicPreservesExistingMode |
| `atomicwrite_test.go` | `TestWriteTicketAtomicRefusesInvalid` | Changed the gate at atomicwrite.go:157 to `len(errs) > 0 && false`, disabling the parse-validation branch entirely so garbage is written over a valid ticket. | TestWriteTicketAtomicRefusesInvalid |
| `atomicwrite_test.go` | `TestWriteTicketAtomicAllowsWhenOriginalInvalid` | Replaced the `if old, readErr := os.ReadFile(path); readErr != nil \|\| len(parseErgErrs(old, path)) == 0 { return ... }` guard at atomicwrite.go:166 with an unconditional `return fmt.Errorf(...)`, i.e. refuse on ANY parse error regardless of the original's state (the original regression). | TestWriteTicketAtomicAllowsWhenOriginalInvalid |
| `atomicwrite_test.go` | `TestWithinStore` | Neutered the rejection in withinStore at atomicwrite.go:65 to `if false && (rel == ".." \|\| strings.HasPrefix(...))`, so withinStore always returns true. (First attempt that deleted the branch failed to compile due to unused `strings`; the &&false form compiles.) | TestWithinStore, TestWithinStore/parent_escape, TestWithinStore/sibling_dir, TestWithinStore/prefix-but-not-subdir, TestWriteTicketAtomicConfinement, TestWriteTicketAtomicConfinementParentEscape |
| `atomicwrite_test.go` | `TestAtomicWriteFileReplacesViaRename` | Added an early `if _, statErr := os.Stat(path); statErr == nil { return os.WriteFile(path, data, perm) }` before CreateTemp at atomicwrite.go:93-94, so an existing target is written in place (same inode) instead of replaced via rename. | TestAtomicWriteFileReplacesViaRename |
| `check_test.go` | `TestHeaderBlankWarnings` | In headerBlankWarnings (check.go:143), negate the detection gate: `if hasInteriorHeaderBlank(data)` -> `if !hasInteriorHeaderBlank(data)`. | TestHeaderBlankWarnings/clean_corpus_yields_no_warnings, TestHeaderBlankWarnings/interior_header_blank_produces_one_warning_naming_the_file, TestHeaderBlankWarnings/warning_shows_basename_even_when_file_is_in_a_subdirectory, TestHeaderBlankWarnings |
| `check_test.go` | `TestHeaderBlankWarnings` | In headerBlankWarnings (check.go:145), replace `filepath.Base(path)` with the full `path` in the warning format args. | TestHeaderBlankWarnings/interior_header_blank_produces_one_warning_naming_the_file, TestHeaderBlankWarnings/warning_shows_basename_even_when_file_is_in_a_subdirectory, TestHeaderBlankWarnings |
| `close_test.go` | `TestAppendLogLine` | close.go:244 strings.Index(content, "\n"+separatorBody) -> strings.LastIndex(...) | TestAppendLogLine, TestAppendLogLine/inserts_before_first_body_separator_when_body_echoes_separator |
| `close_test.go` | `TestRemoveBlockedByLine` | close.go:199 negate the match predicate: && ref.MatchesLocalID(id) -> && !ref.MatchesLocalID(id) (removes non-matching lines, keeps matching ones) | TestRemoveBlockedByLine, TestRemoveBlockedByLine/single_blocker_line_present_is_removed, TestRemoveBlockedByLine/multiple_blocker_lines_for_same_ID_all_removed, TestRemoveBlockedByLine/blocker_line_for_different_ID_preserved, TestRemoveBlockedByLine/trailing_whitespace_on_blocker_line_still_matched, TestRemoveBlockedByLine/whitespace_before_colon_(parser-tolerated_form)_still_matched |
| `close_test.go` | `TestInsertClosedHeader` | close.go:229 negate the blank-line backtrack: strings.TrimSpace(lines[insertAt-1]) == "" -> != "" (scans back over non-blank header lines instead of over blanks) | TestInsertClosedHeader, TestInsertClosedHeader/normal_ticket_with_blank_line_before_log_separator, TestInsertClosedHeader/no_blank_line_before_log_separator, TestInsertClosedHeader/multiple_blank_lines_before_separator_preserved_after_header |
| `config_test.go` | `TestLoadConfig_NoFile` | config.go loadConfig: missing-file branch `return nil, nil` -> `return &Config{}, nil` | TestLoadConfig_NoFile |
| `config_test.go` | `TestConfig_CommentsAndBlankLines` | config.go parseErgrc: removed `cfg.LabelsSection = true` from the `if section == "labels"` block (kept Labels init) | TestConfig_CustomErgrc, TestConfig_EmptyLabelsSection, TestConfig_CommentsAndBlankLines, TestLoadConfig_MinimalLabelsSection |
| `config_test.go` | `TestConfig_Precedence_UpdateURL` | config.go parseErgrc update case: `ok && k == "url"` -> `ok && k != "url"` (url key never stored) | TestConfig_LabelsAbsentUpdatePresent, TestConfig_Precedence_UpdateURL, TestUpdateURL_EnvVarOverridesConfig |
| `config_test.go` | `TestEffectiveLabelSet_WithConfig` | config.go effectiveLabelSet: predicate `cfg != nil && cfg.LabelsSection` -> `cfg != nil && !cfg.LabelsSection` (negated branch selection) | TestConfig_CustomErgrc, TestConfig_EmptyLabelsSection, TestConfig_LabelsAbsentUpdatePresent, TestEffectiveLabelSet_WithConfig, TestEffectiveLabelSet_EmptyLabels |
| `config_test.go` | `TestConfig_CommentsAndBlankLines` | config.go parseErgrc: skip condition `trimmed == "" \|\| strings.HasPrefix(trimmed, "#")` -> `trimmed == ""` (comment skipping dropped) | TestConfig_CommentsAndBlankLines |
| `config_test.go` | `TestConfig_Precedence_UpdateURL` | config.go parseINIKeyValue: value slice `line[idx+1:]` -> `line[idx:]` (off-by-one, includes the '=') | TestConfig_LabelsAbsentUpdatePresent, TestConfig_Precedence_UpdateURL, TestUpdateURL_EnvVarOverridesConfig |
| `contract_test.go` | `TestParseOnce` | check.go cmdCheck: inserted a redundant `tickets, parseErrs = loadErgs(dir)` re-load immediately before validateCorpus (real defect class: corpus loaded twice). | TestParseOnce, TestParseOnce/check_command_parses_each_file_once |
| `contract_test.go` | `TestParseOnce` | list.go loadListEntries: inserted a second `all, _ := loadErgs(dir)` pass to populate knownID (real defect class: redundant re-load for ID resolution). | TestParseOnce, TestParseOnce/list_command_parses_each_file_once |
| `contract_test.go` | `TestLinearOpCount` | validate.go detectCycles: removed the `corpusOpCount++ // per-edge DFS visit` site (the exact site the floor comment names as droppable). Maps to defect class: a per-edge traversal pass is lost. | TestLinearOpCount, TestLinearOpCount/corpus_validation_scales_linearly_with_refs |
| `erg_test.go` | `TestParseHeaderLine` | erg.go parseHeaderLine: dropped the `!isLetter(line[0])` first-char guard (only kept len==0 check) | TestParseHeaderLine/_leading-space:_v, TestParseHeaderLine/1starts-digit:_v, TestParseHeaderLine |
| `erg_test.go` | `TestPathIsClosed` | erg.go pathIsClosed: changed `strings.HasPrefix(lower, "closed-")` to `strings.Contains(lower, "closed")` | TestPathIsClosed/disclosed/0001-foo.erg, TestPathIsClosed |
| `erg_test.go` | `TestMagicLineDowngrade` | erg.go parseErgBytes: changed exact `trimmed == "%erg v1"` to `strings.HasPrefix(trimmed, "%erg v")` | TestMagicLineDowngrade/unknown_v2_emits_generic_missing-magic_error, TestMagicLineDowngrade |
| `erg_test.go` | `TestParseErg` | erg.go parseErgBytes: disabled the BOM-strip branch (`if false && ...`) | TestParseErg/UTF-8_BOM_stripped, TestParseErg |
| `erg_test.go` | `TestParse_BlankLineInsideHeaderBlock` | erg.go blankEndsHeaderBlock: flipped `return false` (header line below blank => block continues) to `return true` | TestParse_BlankLineInsideHeaderBlock, TestParse_BlankBeforeRequiredHeaderFound, TestHasInteriorHeaderBlank/interior_blank_between_header_clusters, TestHasInteriorHeaderBlank/blank_right_after_magic_before_headers, TestHasInteriorHeaderBlank, TestCollapseHeaderBlanks/removes_interior_blank_keeps_terminating_blank, TestCollapseHeaderBlanks |
| `erg_test.go` | `TestClosedWhitespaceDivergence` | erg.go isClosedHeaderLine: changed key from "Closed:" to "Closed" (dropped the colon) | TestClosedWhitespaceDivergence/Closed_space_colon_in_body_does_NOT_trigger_body-section_error, TestClosedWhitespaceDivergence |
| `identity_test.go` | `TestResolveAuthor` | Removed the entire `if v := sanitizeAuthor(os.Getenv("ERG_AUTHOR")); v != "" { return v }` branch so ERG_AUTHOR is never consulted. | TestResolveAuthor/ERG_AUTHOR_wins_over_all, TestResolveAuthor/ERG_AUTHOR_with_embedded_newline_is_sanitized, TestResolveAuthor |
| `identity_test.go` | `TestResolveAuthor` | Removed the `if v := sanitizeAuthor(gitConfigUserName()); v != "" { return v }` branch so git config is never consulted; resolution falls straight from ERG_AUTHOR to USER. | TestResolveAuthor/git_config_wins_when_ERG_AUTHOR_unset, TestResolveAuthor/git_config_with_embedded_CR_is_sanitized, TestResolveAuthor |
| `identity_test.go` | `TestResolveAuthor` | Changed the final `return "unknown"` to `return "anonymous"`. | TestResolveAuthor/unknown_when_all_empty, TestResolveAuthor |
| `identity_test.go` | `TestResolveAuthor` | Turned sanitizeAuthor into a no-op: strings.NewReplacer("\n","","\r","") -> strings.NewReplacer("\n","\n","\r","\r") (the exact distinguishing mutation named in the test comment). | TestResolveAuthor/ERG_AUTHOR_with_embedded_newline_is_sanitized, TestResolveAuthor/git_config_with_embedded_CR_is_sanitized, TestResolveAuthor/USER_with_embedded_newline_is_sanitized, TestResolveAuthor |
| `list_test.go` | `TestListEntryHas` | list.go: in has(), case "open" returns e.closed instead of !e.closed | TestListEntryHas, TestFilterMatches, TestFilterMatches/positive_present |
| `list_test.go` | `TestFilterMatches` | list.go: in matches(), negative loop uses 'if !e.has(n)' instead of 'if e.has(n)' (inverts negative-term semantics) | TestFilterMatches, TestFilterMatches/negative_absent, TestFilterMatches/negative_present, TestFilterMatches/positive_ok_but_negative_present |
| `list_test.go` | `TestReferencesOpenClosed` | list.go: referencesOpenClosed checks only 'if t == "open"', dropping the '\|\| t == "closed"' arm | TestReferencesOpenClosed, TestReferencesOpenClosed/positive_closed, TestReferencesOpenClosed/negative_closed |
| `list_test.go` | `TestBlockedByLabel` | list.go: blockedByLabel returns b.id (not b.ref) in the forge branch | TestBlockedByLabel |
| `list_test.go` | `TestLoadListEntries` | list.go: blocked computed as 'len(blockedBy) >= 0' instead of '> 0' (always blocked) | TestLoadListEntries |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:341 strings.EqualFold(val, "closed") -> EqualFold(val, "open") so a closed ticket is never flagged wasClosed and no Closed: header is appended | TestMigrateFile, TestMigrateFileIdempotent |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:350 rewritten := "Label:"+line[len("Tags:"):] -> "Tag:"+... so Tags: rewrites to Tag: instead of Label: | TestMigrateFile, TestMigrateFileIdempotent |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:316 trimmed == "%erg v1" -> "%erg v2" so the real v1 marker is never matched/rewritten | TestMigrateFile |
| `migrate_test.go` | `TestFoldLogLines` | migrate.go:555 cur += " " + strings.TrimSpace(line) -> cur += "  " + ... (double space), corrupting the fold separator | TestFoldLogLines, TestMigrateFileLogFold |
| `migrate_test.go` | `TestFoldLogLines` | migrate.go:543 splice "T00:00Z" -> "T00:01Z" producing a wrong normalised time | TestFoldLogLines, TestMigrateFileLogFold |
| `migrate_test.go` | `TestFoldLogLinesOrphan` | migrate.go orphan branch changed from emitting the line verbatim to folding it onto cur (cur += " "+line; hasCur=true; folded=true), violating the orphan-untouched invariant | TestFoldLogLinesOrphan, TestMigrateFileOrphanUntouched |
| `migrate_test.go` | `TestMigrateLayoutErgrc` | init.go:20 migrateAssetPaths gains "tickets/.ergrc", so migrateLayout's installAssets call now force-overwrites/creates .ergrc | TestMigrateLayoutErgrc |
| `migrate_test.go` | `TestMigrateErgrc` | migrate.go:488 strings.Replace(line, "[tags]", "[labels]", 1) -> replace with "[label]" (wrong section name) | TestMigrateErgrc |
| `new_test.go` | `TestSlugify` | new.go:26 `if s == ""` -> `if s == "x"` (guard no longer fires on empty) | TestSlugify |
| `new_test.go` | `TestSlugify` | new.go:21 `strings.Trim(s, "-")` -> `strings.TrimRight(s, "-")` (leading hyphen no longer stripped) | TestSlugify |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:142 `if strings.TrimSpace(author) == ""` -> `!= ""` (negate empty-author guard) | TestCmdNewAuthorFlag |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:82 `strings.HasPrefix(a, "-")` -> `strings.HasPrefix(a, "---")` so `--bogus` falls through to positional | TestCmdNewAuthorFlag |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:80 `authorFlag = strings.TrimPrefix(a, "--author=")` -> `authorFlag = a` (whole arg kept) | TestCmdNewAuthorFlag |
| `nextid_test.go` | `TestParseIDFromFilename` | nextid.go:32 changed `n >= 10000` to `n > 10000` (off-by-one on the upper boundary, lets 10000 through) | TestParseIDFromFilename, TestNextID |
| `nextid_test.go` | `TestParseIDFromFilename` | nextid.go:24 replaced `if !strings.HasSuffix(name, ".erg")` guard with `if false` (suffix check removed; any extension parsed) | TestParseIDFromFilename, TestNextID |
| `nextid_test.go` | `TestMaxIDInDir` | nextid.go:47-49 replaced the `if n > maxID { maxID = n }` guard with unconditional `maxID = parseIDFromFilename(d.Name())` (always takes last walked entry instead of the max) | TestMaxIDInDir, TestNextID |
| `nextid_test.go` | `TestNextID` | nextid.go:190 changed `if maxID >= 9999` to `if maxID > 9999` (off-by-one: 9999 no longer triggers errRangeExhausted, returns invalid 10000) | TestNextID |
| `nextid_test.go` | `TestNextID_SkipsSiblingWorktreeUncommittedTicket` | nextid.go:159 changed the sibling-worktree merge `if n > maxID` to `if n < maxID` (sibling IDs never raise the max; can only lower it) | TestNextID_SkipsSiblingWorktreeUncommittedTicket, TestNextID_SiblingWithoutSubdirIsHarmless |
| `nextid_test.go` | `TestNextID_SkipsTicketCommittedOnUncheckedOutBranch` | nextid.go:106 changed maxIDInRef to `return 0` instead of `return maxID` (the per-ref ls-tree result is discarded; Pass 3 contributes nothing) | TestNextID_SkipsTicketCommittedOnUncheckedOutBranch, TestNextID_SkipsTicketOnRemoteTrackingBranch |
| `nextid_test.go` | `TestNextID_SkipsTicketOnRemoteTrackingBranch` | nextid.go:64 dropped the `refs/remotes` argument from the for-each-ref call, leaving only `refs/heads` (remote-tracking branches no longer enumerated) | TestNextID_SkipsTicketOnRemoteTrackingBranch |
| `ref_test.go` | `TestParseRef` | ref.go:31 `if len(raw) == 4 && allDigits(raw)` -> `if len(raw) <= 4 && allDigits(raw)` | TestParseRef/123, TestParseRef |
| `ref_test.go` | `TestParseRef` | ref.go:36-39 removed the `strings.HasPrefix(raw, "gh:")` deprecated-scheme branch entirely | TestParseRef/gh:owner/repo#1, TestParseRef |
| `ref_test.go` | `TestParseRef` | ref.go:98 `if num[0] == '0'` -> `if false && num[0] == '0'` (disables the leading-zero guard, compiles) | TestParseRef/github.com/owner/repo#0, TestParseRef |
| `ref_test.go` | `TestParseRef` | ref.go:61 `if len(parts) == 3` -> `if len(parts) >= 3` | TestParseRef/github.com/owner/repo/extra#1, TestParseRef/github.com/owner/repo/a/b#1, TestParseRef |
| `ref_test.go` | `TestParseRef` | ref.go:62 `host, owner, repo := parts[0], parts[1], parts[2]` -> `parts[0], parts[2], parts[1]` (swap owner/repo) | TestParseRef/github.com/owner/repo#123, TestParseRef/gitlab.com/org/project#7, TestParseRef |
| `refs_test.go` | `TestRefReferencesID` | refs.go:15 — dropped the `id == ""` disjunct from the guard, leaving only `len(refName) < len(id)`. | TestRefReferencesID |
| `refs_test.go` | `TestRefReferencesID` | refs.go:22 — removed the `isRefBoundary(refName, i-1)` conjunct, dropping the left-boundary check. | TestRefReferencesID |
| `refs_test.go` | `TestRefReferencesID` | refs.go:22 — removed the `isRefBoundary(refName, i+len(id))` conjunct, dropping the right-boundary check. | TestRefReferencesID |
| `refs_test.go` | `TestRefReferencesID` | refs.go:34 — removed `c == '-'` from the boundary-char predicate, leaving only '/' and '_'. | TestRefReferencesID |
| `refs_test.go` | `TestRefReferencesID` | refs.go:31 — changed the out-of-range branch in isRefBoundary to `return false` instead of `return true`, treating string edges as non-boundaries. | TestRefReferencesID |
| `refs_git_test.go` | `TestLoadRefMatches` | In refReferencesID, disabled the boundary guard: changed `if isRefBoundary(refName, i-1) && isRefBoundary(refName, i+len(id))` to `if true \|\| (...)` so any substring occurrence matches. | TestLoadRefMatches, TestLoadRefMatches/branches_and_remote-tracking_refs_map_to_ids_with_boundary_check |
| `refs_git_test.go` | `TestLoadGitRefs` | Changed the for-each-ref --format %(then) branch from empty to `%(refname:short)`, so symrefs are emitted instead of dropped. | TestLoadGitRefs, TestLoadGitRefs/local_and_remote-tracking_refs,_HEAD_symref_excluded |
| `refs_git_test.go` | `TestLoadRefMatches` | Removed `\|\| wt.path == top` from the skip condition (kept `_ = top` to compile); the host worktree path is now appended to matches. | TestLoadRefMatches, TestLoadRefMatches/host_worktree_path_excluded_even_when_host_branch_references_id |
| `refs_git_test.go` | `TestLoadRefMatches` | Off-by-one on the left boundary: changed `isRefBoundary(refName, i-1)` to `isRefBoundary(refName, i)`, checking the first id char instead of the preceding boundary. | TestLoadRefMatches, TestLoadRefMatches/branches_and_remote-tracking_refs_map_to_ids_with_boundary_check, TestLoadRefMatches/other_worktree_path_included,_host_worktree_excluded, TestLoadRefMatches/host_worktree_path_excluded_even_when_host_branch_references_id, TestRefReferencesID |
| `resolve_test.go` | `TestResolveDir` | main.go resolveDir: negated the directory guard, `if !info.IsDir()` -> `if info.IsDir()`, so a regular file is accepted and a real directory is rejected. | TestResolveDir, TestResolveDir/explicit_directory, TestResolveDir/explicit_not_a_directory |
| `resolve_test.go` | `TestResolveDirBanner` | main.go resolveDir: flipped the banner guard, `if branch := gitBranch(dir); branch != ""` -> `branch == ""`, suppressing the banner for real repos (and emitting an empty-branch banner for non-repos). | TestResolveDirBanner, TestResolveDirBanner/emits_branch_banner_on_git_repo, TestResolveDirBanner/no_banner_on_non-repo_dir, TestResolveDirBanner/cmdList_--json_stdout_clean, TestResolveDirBanner/cmdReady_emits_branch_banner, TestResolveDirBanner/cmdClose_emits_branch_banner |
| `resolve_test.go` | `TestStoreWorktreeConflict` | main.go storeWorktreeConflict: flipped the equality, `storeTop == cwdTop` -> `storeTop != cwdTop` in the nil-return guard, so different worktrees return nil and same worktrees attempt to error. | TestStoreWorktreeConflict, TestStoreWorktreeConflict/different_worktrees_returns_error, TestStoreWorktreeConflict/same_worktree_returns_nil, TestFindTicketsDir_CrossWorktreeRefused, TestCrossWorktreeCommandRejection, TestCrossWorktreeCommandRejection/cmdList, TestCrossWorktreeCommandRejection/cmdReady, TestCrossWorktreeCommandRejection/cmdNew |
| `resolve_test.go` | `TestResolveTicketByID` | main.go resolveTicketByID: off-by-one on the ambiguity boundary, `if len(matches) > 1` -> `if len(matches) > 2`, so exactly two matches no longer error and the first is returned. | TestResolveTicketByID, TestResolveTicketByID/ambiguous |
| `validate_test.go` | `TestDetectCycles` | In dfs, changed the back-edge predicate `if c == gray` to `if c == black` (validate.go:55), so a gray (on-stack) node is never recognized as closing a cycle. | TestDetectCycles, TestDetectCycles/self-loop_A_blocked-by_A, TestDetectCycles/2-node_cycle_A->B_B->A, TestDetectCycles/3-node_cycle_A->B->C->A, TestDetectCycles/branched_DAG_A->B_A->C_B->D_C->D, TestDetectCycles/multiple_disjoint_cycles |
| `validate_test.go` | `TestValidateErg` | Negated the membership test `if !labelSet[v]` to `if labelSet[v]` (validate.go:89), so unknown labels are accepted and known labels are (spuriously) flagged. | TestValidateErg, TestValidateErg/Label_value_not_in_closed_set, TestValidateErg/Label_post-talk_rejected_(not_in_defaults), TestValidateErg/Label_post-conference_rejected_(not_in_defaults), TestValidateErg_GoldenInvalid, TestValidateErg_GoldenInvalid/0001-bad-label.erg |
| `validate_test.go` | `TestValidateCorpus_GoldenDuplicateIDs` | Off-by-one on the duplicate-count boundary: `if len(files) > 1` -> `if len(files) > 2` (validate.go:138), so exactly-two-file duplicates slip through. | TestValidateCorpus_GoldenDuplicateIDs |
| `validate_test.go` | `TestValidateErg` | Negated the resolution guard `&& !allIDs[ref.ID]` to `&& allIDs[ref.ID]` for Blocked-by (validate.go:157), so dangling refs are no longer flagged. | TestValidateErg, TestValidateErg/Blocked-by_unknown_ID |
| `validate_test.go` | `TestSupersededByCorpus` | Negated the resolution guard `&& !allIDs[ref.ID]` to `&& allIDs[ref.ID]` for Superseded-by (validate.go:171), inverting the rule so resolving refs are flagged and dangling refs accepted. | TestValidateErg, TestValidateErg/Superseded-by_unknown_ID, TestValidateErg_GoldenInvalid, TestValidateErg_GoldenInvalid/0001-bad-superseded-by.erg, TestSupersededByCorpus, TestSupersededByCorpus/closed_carrier_with_resolving_ref_accepted, TestSupersededByCorpus/repeatable:_two_Superseded-by_lines_accepted |
| `validate_test.go` | `TestGlobLocalIDs` | Stored the whole stem instead of the ID prefix: `ids[stem[:idx]]` -> `ids[stem]` (validate.go:198), so '0001-alpha.erg' maps to key '0001-alpha' not '0001'. | TestGlobLocalIDs, TestGlobLocalIDs/extracts_IDs_from_NNNN-slug.erg_files |
| `scaling_test.go` | `TestScalingLinear` | In erg.go loadErgs WalkDir callback, wrapped the per-file parseErg in a `for range pairs` nested loop so each file is re-parsed once per already-seen ticket -> total O(N^2) parses and O(N^2) allocated bytes. | TestScalingLinear, TestScalingLinear/list, TestScalingLinear/ready, TestScalingLinear/close, TestScalingLinear/rm |
| `scaling_test.go` | `TestScalingCorpusValid` | In validate.go validateCorpus, flipped duplicate-ID guard `if len(files) > 1` to `>= 1`, so every distinct ID (each appearing once in a clean corpus) is reported as a duplicate. | TestScalingCorpusValid |
| `scaling_test.go` | `TestScalingLinear` | In check.go cmdCheck, flipped `hasErrors := len(errors) > 0` to `>= 0`, so cmdCheck always returns 1 even with zero validation errors. | TestScalingLinear, TestScalingLinear/check |
| `scaling_test.go` | `TestScalingLinearNegativeControl` | In erg.go loadErgs WalkDir callback, parsed each file but discarded the result (`_ = t; _ = e`) instead of appending to pairs, so loadErgs always returns empty slices -- a real defect dropping all parsed tickets. | TestScalingLinearNegativeControl, TestScalingCorpusValid |
| `resource_test.go` | `TestScalingFDHygiene` | atomicwrite.go:88 -- replaced `f.Close()` with `_ = f` after the O_WRONLY writability probe `os.OpenFile(path, os.O_WRONLY, 0)`, leaking one fd per writeTicketAtomic call. | TestScalingFDHygiene, TestScalingFDHygiene/close |
| `resource_test.go` | `TestScalingFDHygiene` | check.go:233 (inside cmdCheck) -- injected `if leak, lerr := os.Open(dir); lerr == nil { _ = leak }` immediately before `loadErgs(dir)`, leaking one directory fd per check invocation. | TestScalingFDHygiene, TestScalingFDHygiene/check |
| `resource_test.go` | `TestScalingHeapRetention` | erg.go:loadErgs -- added `var loadErgsCache []Erg` package global and `loadErgsCache = append(loadErgsCache, tickets...)` before the return, so every loadErgs call retains its parsed tickets forever. | TestScalingHeapRetention, TestScalingHeapRetention/check, TestScalingHeapRetention/list, TestScalingHeapRetention/ready |

</details>

## 🧬 Oracle strength (free rider — mutants killed per test func)

_A strong oracle (e.g. `== expected`) kills many mutants; a weak one (`!= nil`) kills few. Derived from the mutation data already collected — no extra runs._

| Test func | Mutants killed |
|---|---|
| `ref_test.go::TestParseRef` | 5 |
| `refs_test.go::TestRefReferencesID` | 5 |
| `identity_test.go::TestResolveAuthor` | 4 |
| `migrate_test.go::TestMigrateFile` | 3 |
| `new_test.go::TestCmdNewAuthorFlag` | 3 |
| `refs_git_test.go::TestLoadRefMatches` | 3 |
| `check_test.go::TestHeaderBlankWarnings` | 2 |
| `config_test.go::TestConfig_CommentsAndBlankLines` | 2 |
| `config_test.go::TestConfig_Precedence_UpdateURL` | 2 |
| `contract_test.go::TestParseOnce` | 2 |
| `migrate_test.go::TestFoldLogLines` | 2 |
| `new_test.go::TestSlugify` | 2 |
| `nextid_test.go::TestParseIDFromFilename` | 2 |
| `validate_test.go::TestValidateErg` | 2 |
| `scaling_test.go::TestScalingLinear` | 2 |
| `resource_test.go::TestScalingFDHygiene` | 2 |
| `atomicwrite_test.go::TestAtomicWriteFileRefusesReadOnlyTarget` | 1 |
| `atomicwrite_test.go::TestWriteTicketAtomicPreservesExistingMode` | 1 |
| `atomicwrite_test.go::TestWriteTicketAtomicRefusesInvalid` | 1 |
| `atomicwrite_test.go::TestWriteTicketAtomicAllowsWhenOriginalInvalid` | 1 |
| `atomicwrite_test.go::TestWithinStore` | 1 |
| `atomicwrite_test.go::TestAtomicWriteFileReplacesViaRename` | 1 |
| `close_test.go::TestAppendLogLine` | 1 |
| `close_test.go::TestRemoveBlockedByLine` | 1 |
| `close_test.go::TestInsertClosedHeader` | 1 |
| `config_test.go::TestLoadConfig_NoFile` | 1 |
| `config_test.go::TestEffectiveLabelSet_WithConfig` | 1 |
| `contract_test.go::TestLinearOpCount` | 1 |
| `erg_test.go::TestParseHeaderLine` | 1 |
| `erg_test.go::TestPathIsClosed` | 1 |
| `erg_test.go::TestMagicLineDowngrade` | 1 |
| `erg_test.go::TestParseErg` | 1 |
| `erg_test.go::TestParse_BlankLineInsideHeaderBlock` | 1 |
| `erg_test.go::TestClosedWhitespaceDivergence` | 1 |
| `list_test.go::TestListEntryHas` | 1 |
| `list_test.go::TestFilterMatches` | 1 |
| `list_test.go::TestReferencesOpenClosed` | 1 |
| `list_test.go::TestBlockedByLabel` | 1 |
| `list_test.go::TestLoadListEntries` | 1 |
| `migrate_test.go::TestFoldLogLinesOrphan` | 1 |
| `migrate_test.go::TestMigrateLayoutErgrc` | 1 |
| `migrate_test.go::TestMigrateErgrc` | 1 |
| `nextid_test.go::TestMaxIDInDir` | 1 |
| `nextid_test.go::TestNextID` | 1 |
| `nextid_test.go::TestNextID_SkipsSiblingWorktreeUncommittedTicket` | 1 |
| `nextid_test.go::TestNextID_SkipsTicketCommittedOnUncheckedOutBranch` | 1 |
| `nextid_test.go::TestNextID_SkipsTicketOnRemoteTrackingBranch` | 1 |
| `refs_git_test.go::TestLoadGitRefs` | 1 |
| `resolve_test.go::TestResolveDir` | 1 |
| `resolve_test.go::TestResolveDirBanner` | 1 |
| `resolve_test.go::TestStoreWorktreeConflict` | 1 |
| `resolve_test.go::TestResolveTicketByID` | 1 |
| `validate_test.go::TestDetectCycles` | 1 |
| `validate_test.go::TestValidateCorpus_GoldenDuplicateIDs` | 1 |
| `validate_test.go::TestSupersededByCorpus` | 1 |
| `validate_test.go::TestGlobLocalIDs` | 1 |
| `scaling_test.go::TestScalingCorpusValid` | 1 |
| `scaling_test.go::TestScalingLinearNegativeControl` | 1 |
| `resource_test.go::TestScalingHeapRetention` | 1 |

## 🎯 Diagnosticity (free rider — tests fired per killed mutant)

_On red, does the suite bisect to the cause? FEWER tests firing per killed mutant = sharper diagnosis. Derived from the same data — no extra runs._

| Test file | Test func | Mutation | Tests fired |
|---|---|---|---|
| `atomicwrite_test.go` | `TestAtomicWriteFileRefusesReadOnlyTarget` | Wrapped the O_WRONLY writability probe at atomicwrite.go:87-91 in `if false { ... }`, so the probe never runs and a read-only (0444) target is silently replaced. | 1 |
| `atomicwrite_test.go` | `TestWriteTicketAtomicPreservesExistingMode` | Removed the os.Stat-derived perm at atomicwrite.go:174-177, hardcoding perm := 0644 so the new inode always lands 0644. | 1 |
| `atomicwrite_test.go` | `TestWriteTicketAtomicRefusesInvalid` | Changed the gate at atomicwrite.go:157 to `len(errs) > 0 && false`, disabling the parse-validation branch entirely so garbage is written over a valid ticket. | 1 |
| `atomicwrite_test.go` | `TestWriteTicketAtomicAllowsWhenOriginalInvalid` | Replaced the `if old, readErr := os.ReadFile(path); readErr != nil \|\| len(parseErgErrs(old, path)) == 0 { return ... }` guard at atomicwrite.go:166 with an unconditional `return fmt.Errorf(...)`, i.e. refuse on ANY parse error regardless of the original's state (the original regression). | 1 |
| `atomicwrite_test.go` | `TestAtomicWriteFileReplacesViaRename` | Added an early `if _, statErr := os.Stat(path); statErr == nil { return os.WriteFile(path, data, perm) }` before CreateTemp at atomicwrite.go:93-94, so an existing target is written in place (same inode) instead of replaced via rename. | 1 |
| `config_test.go` | `TestLoadConfig_NoFile` | config.go loadConfig: missing-file branch `return nil, nil` -> `return &Config{}, nil` | 1 |
| `config_test.go` | `TestConfig_CommentsAndBlankLines` | config.go parseErgrc: skip condition `trimmed == "" \|\| strings.HasPrefix(trimmed, "#")` -> `trimmed == ""` (comment skipping dropped) | 1 |
| `list_test.go` | `TestBlockedByLabel` | list.go: blockedByLabel returns b.id (not b.ref) in the forge branch | 1 |
| `list_test.go` | `TestLoadListEntries` | list.go: blocked computed as 'len(blockedBy) >= 0' instead of '> 0' (always blocked) | 1 |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:316 trimmed == "%erg v1" -> "%erg v2" so the real v1 marker is never matched/rewritten | 1 |
| `migrate_test.go` | `TestMigrateLayoutErgrc` | init.go:20 migrateAssetPaths gains "tickets/.ergrc", so migrateLayout's installAssets call now force-overwrites/creates .ergrc | 1 |
| `migrate_test.go` | `TestMigrateErgrc` | migrate.go:488 strings.Replace(line, "[tags]", "[labels]", 1) -> replace with "[label]" (wrong section name) | 1 |
| `new_test.go` | `TestSlugify` | new.go:26 `if s == ""` -> `if s == "x"` (guard no longer fires on empty) | 1 |
| `new_test.go` | `TestSlugify` | new.go:21 `strings.Trim(s, "-")` -> `strings.TrimRight(s, "-")` (leading hyphen no longer stripped) | 1 |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:142 `if strings.TrimSpace(author) == ""` -> `!= ""` (negate empty-author guard) | 1 |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:82 `strings.HasPrefix(a, "-")` -> `strings.HasPrefix(a, "---")` so `--bogus` falls through to positional | 1 |
| `new_test.go` | `TestCmdNewAuthorFlag` | new.go:80 `authorFlag = strings.TrimPrefix(a, "--author=")` -> `authorFlag = a` (whole arg kept) | 1 |
| `nextid_test.go` | `TestNextID` | nextid.go:190 changed `if maxID >= 9999` to `if maxID > 9999` (off-by-one: 9999 no longer triggers errRangeExhausted, returns invalid 10000) | 1 |
| `nextid_test.go` | `TestNextID_SkipsTicketOnRemoteTrackingBranch` | nextid.go:64 dropped the `refs/remotes` argument from the for-each-ref call, leaving only `refs/heads` (remote-tracking branches no longer enumerated) | 1 |
| `refs_test.go` | `TestRefReferencesID` | refs.go:15 — dropped the `id == ""` disjunct from the guard, leaving only `len(refName) < len(id)`. | 1 |
| `refs_test.go` | `TestRefReferencesID` | refs.go:22 — removed the `isRefBoundary(refName, i-1)` conjunct, dropping the left-boundary check. | 1 |
| `refs_test.go` | `TestRefReferencesID` | refs.go:22 — removed the `isRefBoundary(refName, i+len(id))` conjunct, dropping the right-boundary check. | 1 |
| `refs_test.go` | `TestRefReferencesID` | refs.go:34 — removed `c == '-'` from the boundary-char predicate, leaving only '/' and '_'. | 1 |
| `refs_test.go` | `TestRefReferencesID` | refs.go:31 — changed the out-of-range branch in isRefBoundary to `return false` instead of `return true`, treating string edges as non-boundaries. | 1 |
| `validate_test.go` | `TestValidateCorpus_GoldenDuplicateIDs` | Off-by-one on the duplicate-count boundary: `if len(files) > 1` -> `if len(files) > 2` (validate.go:138), so exactly-two-file duplicates slip through. | 1 |
| `scaling_test.go` | `TestScalingCorpusValid` | In validate.go validateCorpus, flipped duplicate-ID guard `if len(files) > 1` to `>= 1`, so every distinct ID (each appearing once in a clean corpus) is reported as a duplicate. | 1 |
| `close_test.go` | `TestAppendLogLine` | close.go:244 strings.Index(content, "\n"+separatorBody) -> strings.LastIndex(...) | 2 |
| `contract_test.go` | `TestParseOnce` | check.go cmdCheck: inserted a redundant `tickets, parseErrs = loadErgs(dir)` re-load immediately before validateCorpus (real defect class: corpus loaded twice). | 2 |
| `contract_test.go` | `TestParseOnce` | list.go loadListEntries: inserted a second `all, _ := loadErgs(dir)` pass to populate knownID (real defect class: redundant re-load for ID resolution). | 2 |
| `contract_test.go` | `TestLinearOpCount` | validate.go detectCycles: removed the `corpusOpCount++ // per-edge DFS visit` site (the exact site the floor comment names as droppable). Maps to defect class: a per-edge traversal pass is lost. | 2 |
| `erg_test.go` | `TestPathIsClosed` | erg.go pathIsClosed: changed `strings.HasPrefix(lower, "closed-")` to `strings.Contains(lower, "closed")` | 2 |
| `erg_test.go` | `TestMagicLineDowngrade` | erg.go parseErgBytes: changed exact `trimmed == "%erg v1"` to `strings.HasPrefix(trimmed, "%erg v")` | 2 |
| `erg_test.go` | `TestParseErg` | erg.go parseErgBytes: disabled the BOM-strip branch (`if false && ...`) | 2 |
| `erg_test.go` | `TestClosedWhitespaceDivergence` | erg.go isClosedHeaderLine: changed key from "Closed:" to "Closed" (dropped the colon) | 2 |
| `identity_test.go` | `TestResolveAuthor` | Changed the final `return "unknown"` to `return "anonymous"`. | 2 |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:341 strings.EqualFold(val, "closed") -> EqualFold(val, "open") so a closed ticket is never flagged wasClosed and no Closed: header is appended | 2 |
| `migrate_test.go` | `TestMigrateFile` | migrate.go:350 rewritten := "Label:"+line[len("Tags:"):] -> "Tag:"+... so Tags: rewrites to Tag: instead of Label: | 2 |
| `migrate_test.go` | `TestFoldLogLines` | migrate.go:555 cur += " " + strings.TrimSpace(line) -> cur += "  " + ... (double space), corrupting the fold separator | 2 |
| `migrate_test.go` | `TestFoldLogLines` | migrate.go:543 splice "T00:00Z" -> "T00:01Z" producing a wrong normalised time | 2 |
| `migrate_test.go` | `TestFoldLogLinesOrphan` | migrate.go orphan branch changed from emitting the line verbatim to folding it onto cur (cur += " "+line; hasCur=true; folded=true), violating the orphan-untouched invariant | 2 |
| `nextid_test.go` | `TestParseIDFromFilename` | nextid.go:32 changed `n >= 10000` to `n > 10000` (off-by-one on the upper boundary, lets 10000 through) | 2 |
| `nextid_test.go` | `TestParseIDFromFilename` | nextid.go:24 replaced `if !strings.HasSuffix(name, ".erg")` guard with `if false` (suffix check removed; any extension parsed) | 2 |
| `nextid_test.go` | `TestMaxIDInDir` | nextid.go:47-49 replaced the `if n > maxID { maxID = n }` guard with unconditional `maxID = parseIDFromFilename(d.Name())` (always takes last walked entry instead of the max) | 2 |
| `nextid_test.go` | `TestNextID_SkipsSiblingWorktreeUncommittedTicket` | nextid.go:159 changed the sibling-worktree merge `if n > maxID` to `if n < maxID` (sibling IDs never raise the max; can only lower it) | 2 |
| `nextid_test.go` | `TestNextID_SkipsTicketCommittedOnUncheckedOutBranch` | nextid.go:106 changed maxIDInRef to `return 0` instead of `return maxID` (the per-ref ls-tree result is discarded; Pass 3 contributes nothing) | 2 |
| `ref_test.go` | `TestParseRef` | ref.go:31 `if len(raw) == 4 && allDigits(raw)` -> `if len(raw) <= 4 && allDigits(raw)` | 2 |
| `ref_test.go` | `TestParseRef` | ref.go:36-39 removed the `strings.HasPrefix(raw, "gh:")` deprecated-scheme branch entirely | 2 |
| `ref_test.go` | `TestParseRef` | ref.go:98 `if num[0] == '0'` -> `if false && num[0] == '0'` (disables the leading-zero guard, compiles) | 2 |
| `refs_git_test.go` | `TestLoadRefMatches` | In refReferencesID, disabled the boundary guard: changed `if isRefBoundary(refName, i-1) && isRefBoundary(refName, i+len(id))` to `if true \|\| (...)` so any substring occurrence matches. | 2 |
| `refs_git_test.go` | `TestLoadGitRefs` | Changed the for-each-ref --format %(then) branch from empty to `%(refname:short)`, so symrefs are emitted instead of dropped. | 2 |
| `refs_git_test.go` | `TestLoadRefMatches` | Removed `\|\| wt.path == top` from the skip condition (kept `_ = top` to compile); the host worktree path is now appended to matches. | 2 |
| `resolve_test.go` | `TestResolveTicketByID` | main.go resolveTicketByID: off-by-one on the ambiguity boundary, `if len(matches) > 1` -> `if len(matches) > 2`, so exactly two matches no longer error and the first is returned. | 2 |
| `validate_test.go` | `TestValidateErg` | Negated the resolution guard `&& !allIDs[ref.ID]` to `&& allIDs[ref.ID]` for Blocked-by (validate.go:157), so dangling refs are no longer flagged. | 2 |
| `validate_test.go` | `TestGlobLocalIDs` | Stored the whole stem instead of the ID prefix: `ids[stem[:idx]]` -> `ids[stem]` (validate.go:198), so '0001-alpha.erg' maps to key '0001-alpha' not '0001'. | 2 |
| `scaling_test.go` | `TestScalingLinear` | In check.go cmdCheck, flipped `hasErrors := len(errors) > 0` to `>= 0`, so cmdCheck always returns 1 even with zero validation errors. | 2 |
| `scaling_test.go` | `TestScalingLinearNegativeControl` | In erg.go loadErgs WalkDir callback, parsed each file but discarded the result (`_ = t; _ = e`) instead of appending to pairs, so loadErgs always returns empty slices -- a real defect dropping all parsed tickets. | 2 |
| `resource_test.go` | `TestScalingFDHygiene` | atomicwrite.go:88 -- replaced `f.Close()` with `_ = f` after the O_WRONLY writability probe `os.OpenFile(path, os.O_WRONLY, 0)`, leaking one fd per writeTicketAtomic call. | 2 |
| `resource_test.go` | `TestScalingFDHygiene` | check.go:233 (inside cmdCheck) -- injected `if leak, lerr := os.Open(dir); lerr == nil { _ = leak }` immediately before `loadErgs(dir)`, leaking one directory fd per check invocation. | 2 |
| `check_test.go` | `TestHeaderBlankWarnings` | In headerBlankWarnings (check.go:145), replace `filepath.Base(path)` with the full `path` in the warning format args. | 3 |
| `config_test.go` | `TestConfig_Precedence_UpdateURL` | config.go parseErgrc update case: `ok && k == "url"` -> `ok && k != "url"` (url key never stored) | 3 |
| `config_test.go` | `TestConfig_Precedence_UpdateURL` | config.go parseINIKeyValue: value slice `line[idx+1:]` -> `line[idx:]` (off-by-one, includes the '=') | 3 |
| `erg_test.go` | `TestParseHeaderLine` | erg.go parseHeaderLine: dropped the `!isLetter(line[0])` first-char guard (only kept len==0 check) | 3 |
| `identity_test.go` | `TestResolveAuthor` | Removed the entire `if v := sanitizeAuthor(os.Getenv("ERG_AUTHOR")); v != "" { return v }` branch so ERG_AUTHOR is never consulted. | 3 |
| `identity_test.go` | `TestResolveAuthor` | Removed the `if v := sanitizeAuthor(gitConfigUserName()); v != "" { return v }` branch so git config is never consulted; resolution falls straight from ERG_AUTHOR to USER. | 3 |
| `list_test.go` | `TestListEntryHas` | list.go: in has(), case "open" returns e.closed instead of !e.closed | 3 |
| `list_test.go` | `TestReferencesOpenClosed` | list.go: referencesOpenClosed checks only 'if t == "open"', dropping the '\|\| t == "closed"' arm | 3 |
| `ref_test.go` | `TestParseRef` | ref.go:61 `if len(parts) == 3` -> `if len(parts) >= 3` | 3 |
| `ref_test.go` | `TestParseRef` | ref.go:62 `host, owner, repo := parts[0], parts[1], parts[2]` -> `parts[0], parts[2], parts[1]` (swap owner/repo) | 3 |
| `resolve_test.go` | `TestResolveDir` | main.go resolveDir: negated the directory guard, `if !info.IsDir()` -> `if info.IsDir()`, so a regular file is accepted and a real directory is rejected. | 3 |
| `check_test.go` | `TestHeaderBlankWarnings` | In headerBlankWarnings (check.go:143), negate the detection gate: `if hasInteriorHeaderBlank(data)` -> `if !hasInteriorHeaderBlank(data)`. | 4 |
| `close_test.go` | `TestInsertClosedHeader` | close.go:229 negate the blank-line backtrack: strings.TrimSpace(lines[insertAt-1]) == "" -> != "" (scans back over non-blank header lines instead of over blanks) | 4 |
| `config_test.go` | `TestConfig_CommentsAndBlankLines` | config.go parseErgrc: removed `cfg.LabelsSection = true` from the `if section == "labels"` block (kept Labels init) | 4 |
| `identity_test.go` | `TestResolveAuthor` | Turned sanitizeAuthor into a no-op: strings.NewReplacer("\n","","\r","") -> strings.NewReplacer("\n","\n","\r","\r") (the exact distinguishing mutation named in the test comment). | 4 |
| `list_test.go` | `TestFilterMatches` | list.go: in matches(), negative loop uses 'if !e.has(n)' instead of 'if e.has(n)' (inverts negative-term semantics) | 4 |
| `resource_test.go` | `TestScalingHeapRetention` | erg.go:loadErgs -- added `var loadErgsCache []Erg` package global and `loadErgsCache = append(loadErgsCache, tickets...)` before the return, so every loadErgs call retains its parsed tickets forever. | 4 |
| `config_test.go` | `TestEffectiveLabelSet_WithConfig` | config.go effectiveLabelSet: predicate `cfg != nil && cfg.LabelsSection` -> `cfg != nil && !cfg.LabelsSection` (negated branch selection) | 5 |
| `refs_git_test.go` | `TestLoadRefMatches` | Off-by-one on the left boundary: changed `isRefBoundary(refName, i-1)` to `isRefBoundary(refName, i)`, checking the first id char instead of the preceding boundary. | 5 |
| `scaling_test.go` | `TestScalingLinear` | In erg.go loadErgs WalkDir callback, wrapped the per-file parseErg in a `for range pairs` nested loop so each file is re-parsed once per already-seen ticket -> total O(N^2) parses and O(N^2) allocated bytes. | 5 |
| `atomicwrite_test.go` | `TestWithinStore` | Neutered the rejection in withinStore at atomicwrite.go:65 to `if false && (rel == ".." \|\| strings.HasPrefix(...))`, so withinStore always returns true. (First attempt that deleted the branch failed to compile due to unused `strings`; the &&false form compiles.) | 6 |
| `close_test.go` | `TestRemoveBlockedByLine` | close.go:199 negate the match predicate: && ref.MatchesLocalID(id) -> && !ref.MatchesLocalID(id) (removes non-matching lines, keeps matching ones) | 6 |
| `resolve_test.go` | `TestResolveDirBanner` | main.go resolveDir: flipped the banner guard, `if branch := gitBranch(dir); branch != ""` -> `branch == ""`, suppressing the banner for real repos (and emitting an empty-branch banner for non-repos). | 6 |
| `validate_test.go` | `TestDetectCycles` | In dfs, changed the back-edge predicate `if c == gray` to `if c == black` (validate.go:55), so a gray (on-stack) node is never recognized as closing a cycle. | 6 |
| `validate_test.go` | `TestValidateErg` | Negated the membership test `if !labelSet[v]` to `if labelSet[v]` (validate.go:89), so unknown labels are accepted and known labels are (spuriously) flagged. | 6 |
| `erg_test.go` | `TestParse_BlankLineInsideHeaderBlock` | erg.go blankEndsHeaderBlock: flipped `return false` (header line below blank => block continues) to `return true` | 7 |
| `validate_test.go` | `TestSupersededByCorpus` | Negated the resolution guard `&& !allIDs[ref.ID]` to `&& allIDs[ref.ID]` for Superseded-by (validate.go:171), inverting the rule so resolving refs are flagged and dangling refs accepted. | 7 |
| `resolve_test.go` | `TestStoreWorktreeConflict` | main.go storeWorktreeConflict: flipped the equality, `storeTop == cwdTop` -> `storeTop != cwdTop` in the nil-return guard, so different worktrees return nil and same worktrees attempt to error. | 8 |

## Per-file summaries

- **`src/go/atomicwrite_test.go`** (6 caught) · _has negative control_ — Baseline green (426 tests pass). Audited atomicwrite_test.go against its sole mutation target atomicwrite.go. Applied 6 minimal, compiling, behavior-changing mutations across the file's distinct data-safety invariants: read-only-target refusal, mode preservation, validate-before-replace, the already-invalid bypass, store-confinement, and the temp-then-rename atomic property. All 6 were CAUGHT by a Test* defined in this same file (sameFileCaught=true for every finding). No survivors, no equivalents. This test file has real fangs. Negative controls are present and meaningful: TestAtomicWriteFileLeavesNoTemp, TestWriteTicketAtomicHappyPath, and the within-case 'happy' assertions ensure the guards refuse the bad case rather than everything. Note: TestCreateExclusiveNoClobber exercises createExclusive which lives in new.go (a sibling), not in the mutation target, so it was not mutated here.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-3/src/go/check_test.go`** (2 caught, 1 survived) — Baseline green (426 tests). check_test.go defines one Test* func, TestHeaderBlankWarnings (3 subtests), exercising headerBlankWarnings in check.go. Three behaviors probed. Detection-gate inversion (caught: pins both polarities), basename-only path-leak guard (caught: the explicit class guard at lines 35/58 trips on os.PathSeparator), and the .erg suffix scoping filter (SURVIVED). The suffix filter is toothless: dropping it so the walk reads every file passes the whole suite, because no fixture ever places a non-.erg file with erg-shaped header text. A notes.txt with an interior-header-blank body yields nil pristine but a spurious 'WARN notes.txt: ...' under the mutant. Fix: add a subtest writing a non-.erg file alongside an .erg blank and assert only the .erg is reported, with a red-control proving it fails when the filter is removed. The file has real fangs on detection and path-leak; its scoping invariant is unguarded.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-4/src/go/close_test.go`** (3 caught) — close_test.go has real fangs. All three Test* functions (TestAppendLogLine, TestRemoveBlockedByLine, TestInsertClosedHeader) each caught a minimal, compiling, behavior-changing mutation of the real defect class they exist to guard: Index-vs-LastIndex for the log-insertion point, the Blocked-by match predicate, and the blank-line backtrack boundary in header insertion. Baseline green (426 tests). No mutations survived; every catch came from the same-file test (no sibling-only catches), so the file earns full fang credit. No negative-control test is present in this file, but the tests' assertions cover both positive and negative directions (e.g. matching line removed AND different-ID line preserved), which substitutes for an explicit control. The one source code path not mutation-probed is insertClosedHeader's missing-separator error branch, which the wantErr subtest already exercises directly via err-nil and message-substring assertions.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-5/src/go/config_test.go`** (6 caught) — Baseline green (426 tests pass, no build tags). config_test.go is the same-file owner for all 13 Test* funcs. I applied 6 minimal, compiling, behavior-changing mutations to config.go covering every distinct behavior the suite claims to enforce: missing-file nil-config fallback, [labels]-section detection (LabelsSection flag), [update] url key parsing, effectiveLabelSet branch selection, comment-line skipping, and the INI key/value split boundary. ALL 6 were caught by tests defined in config_test.go (sameFileCaught=true for each) — no survivors, no equivalents, no compile errors. This test file has real fangs across its full behavioral surface; no toothless tests and no suggestions needed. Note: resolveUpdateRemote (exercised by TestUpdateURL_EnvVarOverridesConfig) lives in update.go which was outside the mutate scope, so its precedence logic was not directly mutated, but the config-side UpdateURL plumbing feeding it was and is well-guarded.
- **`src/go/contract_test.go`** (3 caught, 1 survived, 2 skipped) · _has negative control_ — Baseline green (426 tests pass). OWN_FUNCS = TestParseOnce, TestLinearOpCount. This is an instrumentation-counter contract test: TestParseOnce asserts parseCount==n (parse each file once) across loadErgs/cmdCheck/cmdList; TestLinearOpCount asserts corpusOpCount scales linearly with both a ratio band (1.5-2.5) and an absolute floor (5n-3). It carries two genuine canaries (double-parse trips parseCount; quadratic ref fan-out trips the ratio), both of which pass at baseline and prove the instrumentation is not vacuous. Real fangs confirmed (3 caught): - Redundant corpus load in cmdCheck (check.go) -> TestParseOnce red (parseCount 30 vs 15). - Redundant corpus load in loadListEntries (list.go) -> TestParseOnce red (parseCount 30 vs 15). - Dropping the per-edge DFS counter site in detectCycles (validate.go) -> TestLinearOpCount floor red (198 vs >=247). The floor catches a single dropped site that the ratio test alone would miss. Note: the real parse-once logic + parseCount++ live in erg.go (loadErgs/parseErgBytes), which is outside the assigned mutation targets; the parse-once mutations were therefore expressed as redundant loads inside the assigned files (check.go/list.go), a legitimate defect class the test does catch. One toothless gap (1 survived): replacing the O(1) map lookup in validateCorpus rule-10 Blocked-by resolution with a linear scan (making it quadratic) survives untouched. corpusOpCount is incremented once PER REF (outside the lookup), so it stays linear while real CPU work goes quadratic; the recorded ratio remains ~2.0 and the floor is still met. The test file explicitly concedes this defect class is delegated to a wall-clock backstop in test_contract.sh, OUTSIDE the Go default suite -- so within contract_test.go it is a real blind spot. Fix: add a per-comparison counter inside the lookup (deterministic, stays in the default suite) or a timing/allocation backstop, asserting that the inside-a-single-ref work scales linearly.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-7/src/go/erg_test.go`** (6 caught) · _has negative control_ — Baseline green (426 tests pass). erg_test.go has strong fangs against erg.go. All six behavior-changing mutations applied to mapped erg.go functions (parseHeaderLine first-char guard, pathIsClosed prefix-vs-substring boundary, legacy-v1 exact-match magic detection, BOM strip, blankEndsHeaderBlock interior-blank predicate, isClosedHeaderLine exact-prefix match) were caught by Test* functions defined in this same file — no survivors, no equivalents. Note: many other Test* funcs in this file (TestStaleBlockedBy, TestOpenCarrierSupersededBy, TestFolderClosure, TestStrayGoSource, TestEncodingWarnings, TestDispatchRegistrySync, TestMutating/ScopeConfinementCMDSCoverage) target sibling sources (check.go, main.go, helptext.go), not erg.go, and so were not exercised by these erg.go mutations; their fangs would need a separate audit of those targets. The two CMDS-coverage meta-tests carry explicit anti-tautology negative controls (inject \"frobnicate\", assert it is reported missing).
- **`src/go/identity_test.go`** (4 caught) — Baseline green (426 tests). identity_test.go defines a single Test func, TestResolveAuthor, with 7 subtests covering the 3-level precedence chain (ERG_AUTHOR > git config > USER > "unknown") plus a security-class CRLF-sanitization guard exercised on all three input paths. Applied 4 minimal compiling, behavior-changing mutations to identity.go — dropping the ERG_AUTHOR branch, dropping the git-config branch, changing the "unknown" fallback constant, and neutering sanitizeAuthor (the no-op Replacer mutation the test comment explicitly names). All 4 were caught by the same-file TestResolveAuthor. No survivors, no equivalents, no compile errors. This test has real fangs across both precedence ordering and the header-injection security invariant.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-9/src/go/list_test.go`** (5 caught, 1 equivalent) — Default-suite test list_test.go has real fangs on 5 of 6 audited behaviors. baselineGreen=true (426 tests pass). 5 mutations were caught by own-file tests (sameFileCaught=true): the open==!closed pseudo-label (TestListEntryHas), negative-term NONE-of semantics (TestFilterMatches), the open\|closed state reference (TestReferencesOpenClosed), forge-vs-local blocker label rendering (TestBlockedByLabel), and the blocked boundary len>0 (TestLoadListEntries). One mutation SURVIVED: dropping the 'arg == \"..\"' literal fast-path in isDirArg. The TestIsDirArg case {\"parent dir\", \"..\", true} still passes because os.Stat(\"..\") succeeds against the real parent of the test cwd, so the os.Stat fallback masks the deleted branch — the literal '..' guard has no fang. Suggested fix: a negative control proving '..' classification is filesystem-independent, or demote '..' to a documented redundancy. No dedicated negative-control test exists in the file (hasNegativeControl=false).
- **`src/go/migrate_test.go`** (8 caught) — All 8 mutations across distinct behaviors were CAUGHT by tests defined in migrate_test.go (baseline green: 426 tests). The file has strong fangs: every defect class the tests claim to enforce produced a same-file failure. Mutations targeted: (1) Status: closed -> Closed-header detection (EqualFold "closed"->"open"), (2) legacy Tags:->Label: key rewrite, (3) %erg v1 magic-line rewrite, (4) log-continuation single-space join, (5) date-only stamp T00:00Z value, (6) orphan-not-folded invariant (orphan emitted verbatim before first entry), (7) migrateAssetPaths .ergrc exclusion (ticket 0224 — defined in init.go but consumed by migrateLayout in migrate.go and guarded by TestMigrateLayoutErgrc), (8) .ergrc [tags]->[labels] section-header rewrite. No equivalent or surviving mutants. One mutation (#7) lives in the adjacent init.go because the behavior under test (migrateLayout must not touch .ergrc) is parameterized by migrateAssetPaths declared there; the consuming function migrateLayout and its test are both in the migrate.go/migrate_test.go pair, and the same-file test caught it. No negative-control/canary test exists in this file, but it is not needed — every behavior has a fang.
- **`src/go/new_test.go`** (1 survived, 5 caught) — Baseline green (426 tests pass). new_test.go defines 2 Test* funcs: TestSlugify and TestCmdNewAuthorFlag. Six behavior-changing mutations applied to new.go (the only mutable target). Five were CAUGHT by an own-file test (real fangs): the empty-slug->'untitled' guard, leading-hyphen trim (Trim vs TrimRight), the empty/whitespace --author rejection, unknown-flag rejection, and --author= prefix stripping. ONE SURVIVED: the slugify 40-char truncation boundary (`> 40` -> `> 41`) is never probed at its off-by-one edge — the test's only long input is 65 chars, deep inside the truncation region, so the assertion can't tell `> 40` from `> 41`. Fix: add an exactly-41-char and exactly-40-char boundary case. Note: the author newline-stripping behavior the test also checks is implemented in sanitizeAuthor (identity.go, a sibling file outside the allowed mutation scope), so it was not mutated. No negative-control test exists in this file, but the empty/whitespace --author subtests function as guard-trip checks for that one invariant.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-12/src/go/nextid_test.go`** (7 caught) — nextid_test.go has strong fangs: all 7 behavior-distinct mutations applied to nextid.go were caught by Test* functions defined in this same file (sameFileCaught=true for every finding). The file exercises the full three-pass ID-allocation scan (local FS walk, sibling worktrees, branch/remote-tracking ls-tree) plus the parse and boundary logic, and each pass has a dedicated test that goes red when its source is broken. Coverage highlights: both 4-digit boundaries (parseIDFromFilename >=10000 and formatNextID >=9999) are pinned by exact-value cases; the remote-tracking pass is independently isolated (dropping only refs/remotes fails exactly one test); Pass 2 and Pass 3 are separately covered by git-backed integration tests. baselineGreen=true (426 tests pass). No negative-control / canary test exists in the file, but the suite's discriminating power is demonstrated directly by the mutation results. Note: the git-backed tests (TestNextID_Skips*, TestNextID_SiblingWithoutSubdirIsHarmless) use exec/subprocess and t.Skip if git is absent; per project conventions these would ideally carry @integration-equivalent build tagging, but they ran and caught mutations here.
- **`src/go/ref_test.go`** (5 caught) — ref_test.go contains a single table-driven test, TestParseRef, exercising parseRef in ref.go. Baseline green (426 passed). Applied 5 minimal compiling behavior-changing mutations spanning the distinct defect classes the table claims to enforce: the 4-digit-local boundary, the deprecated-vs-case-sensitive error-message contract, the leading-zero rejection in validateIssueNumber, the exactly-3-part forge-path arity, and the owner/repo field-extraction mapping. ALL 5 were caught by TestParseRef (the same-file own func) — the table tests both error presence AND precise errContains substrings AND extracted field values, giving it real fangs. No survived or equivalent findings. No negative-control test exists in the file, but it is not needed: the error-message-substring assertions (cases at lines 40/43/47/48) act as positive guards that pin down which failure mode fires, and they killed the gh:-branch-removal mutation that a presence-only check would have missed.
- **`src/go/refs_test.go`** (5 caught) — refs_test.go contains one Test* func (TestRefReferencesID), a table-driven test of the pure predicate refReferencesID and its helper isRefBoundary. Baseline green (426 tests pass, no build tags). I applied 5 minimal compiling, behavior-changing mutations covering every distinct invariant the test claims to enforce: the empty-id short-circuit, the left word-boundary check, the right word-boundary check, the '-' delimiter membership in isRefBoundary, and the start/end-of-string boundary convention. All 5 were caught by TestRefReferencesID (sameFileCaught=true) — this test has real fangs, with explicit positive and negative (word-boundary) rows and dedicated empty-id pathological cases. No survived/equivalent findings. Note: the other functions in refs.go (loadGitRefs, loadGitWorktrees, worktreeTopFor, gitBranch, loadRefMatches) shell out to git and are not exercised by this test file; they are out of scope for refs_test.go's pure-logic coverage. No negative-control test exists in the file, but it is not needed here since every fang fired.
- **`/home/haduong/git-erg/.claude/worktrees/wf_356ca0b7-db6-15/src/go/refs_git_test.go`** (4 caught) — refs_git_test.go has real fangs. OWN_FUNCS = TestLoadGitRefs, TestLoadRefMatches. Baseline green (426 tests pass). Audited 4 distinct behaviors with minimal compiling, behavior-changing mutations to refs.go: (1) word-boundary substring guard in refReferencesID, (2) remote-HEAD-symref exclusion in loadGitRefs, (3) host-worktree path suppression (wt.path == top), (4) left-boundary index arithmetic. All 4 mutations were CAUGHT by own-file tests (sameFileCaught=true in every case). The host-worktree case is notably well-tested via a dedicated distinguishing subtest that puts the host branch on the same id. No survived/equivalent/toothless findings. Mutation 4 was additionally caught by sibling TestRefReferencesID (refs_test.go), but own-file coverage stands on its own.
- **`src/go/resolve_test.go`** (4 caught) — resolve_test.go has strong fangs. All 4 behavior-changing mutations to main.go (the IsDir validation in resolveDir, the branch-banner emission guard, the worktree-conflict comparison in storeWorktreeConflict, and the ambiguity boundary in resolveTicketByID) were caught by tests defined IN THIS FILE. baselineGreen=true (426 pass). No equivalent or survived mutants found; no canary/negative-control test present in the file (none needed given all fangs verified). sameFileCaught=true for every finding.
- **`src/go/tag_test.go`** (1 skipped) — SKIPPED — the assigned test file src/go/tag_test.go and source target src/go/tag.go do not exist in the current tree. They were renamed to label_test.go / label.go in commits da3fcef and bfab73f (ticket 0175, "rename Tag header to Label across spec, source, corpus"; "migrate test suites and docs to Label terminology"). The package baseline is green (426 tests pass via `cd src/go && go test -count=1 ./...`), but there is no tag.go to mutate, so no mutation testing could be performed. If the intent was to audit the label-terminology test, the correct target is src/go/label_test.go against src/go/label.go.
- **`/home/haduong/git-erg/src/go/validate_test.go`** (6 caught) · _has negative control_ — Baseline green (426 tests). validate_test.go has strong fangs: all 6 behavior-changing mutations applied to validate.go (the only mutable target) were caught by Test* functions defined IN this file — detectCycles back-edge predicate, label-vocabulary membership, duplicate-ID boundary, rule-10 Blocked-by resolution, rule-15 Superseded-by resolution, and globLocalIDs ID extraction. No survivors, no equivalents. Each mutation was a real defect-class representative (boundary flip, predicate negation, off-by-one index, wrong-substring) and was COMPILING; pristine restored via `git checkout --` between every mutation and final baseline re-verified green. The file also ships its own negative control: TestV1HeaderKeys_FixtureCoverage injects a bogus header key and asserts the coverage check reports the gap, proving that meta-test is not a tautology. NOTE: many Test* funcs in this file (parser-rule cases in TestValidateErg, TestRequiredHeaderEmptyValueRejected, TestTitleStatusWordRule, etc.) actually exercise parseErg/ergspec code in sibling source files, which were out of scope per the assignment (mutable target: validate.go only); those behaviors were not mutated. The work was done in a throwaway detached worktree of /home/haduong/git-erg (the assigned db6-18 worktree is the unrelated IDH harness repo and does not contain src/go).
- **`/home/haduong/git-erg/src/go/scaling_test.go`** (4 caught) · _has negative control_ — Baseline green with -tags scaling. The scaling guard (scaling_test.go, ticket 0159) defines 3 Test* funcs; all 3 demonstrated real fangs and TestScalingLinearNegativeControl was proven non-vacuous. Canary (loadErgs O(N^2) re-parse) caught -> ratio ~16x trips TestScalingLinear. Three further mutations all caught by an OWN_FUNC: (1) duplicate-ID boundary >1 -> >=1 in validateCorpus caught by TestScalingCorpusValid; (2) cmdCheck always-fail >0 -> >=0 caught by TestScalingLinear's per-command exit-code preflight; (3) loadErgs dropping all tickets caught by both TestScalingLinearNegativeControl (control ratio collapses to 3.77) and TestScalingCorpusValid. No survived/toothless assertions; no equivalent mutations. The guard's documented blind spot (pure compute-bound O(N^2) allocating nothing) is explicitly owned by the 0154 wall-clock backstop + review and is out of this allocation-volume guard's domain by design. Environment note: the assigned worktree is the IDH-harness checkout and has no src/go/; the target files live in the git-erg project at /home/haduong/git-erg/src/go/, mutated safely in throwaway worktree /tmp/fang-erg-23.
- **`/home/haduong/git-erg/src/go/resource_test.go`** (3 caught, 2 equivalent) · _has negative control_ — resource_test.go (package main, build tag `scaling`) is the resource-hygiene guard sibling to scaling_test.go (ticket 0169). Baseline: 446 tests pass under -tags scaling. The file defines 4 Test* funcs: two guards (TestScalingFDHygiene, TestScalingHeapRetention) and two falsifiable negative controls. Work was done in a private throwaway git-erg worktree (the IDH-harness worktree I was spawned into does not contain the git-erg sources; an existing /tmp/fang-erg-wt looked shared so I created /tmp/fang-erg-356ca0b7 to avoid clobbering a sibling agent's reverts).\n\nAll three behavior-changing source mutations were CAUGHT by a same-file test:\n1. CANARY -- dropped f.Close() on the writability probe in atomicwrite.go (close/rm write path) -> TestScalingFDHygiene/close red (8->10 fds). Canary correctly classifies as caught; harness is correctly wired.\n2. Injected an unclosed os.Open into cmdCheck (read path) -> TestScalingFDHygiene/check red (8->9).\n3. Injected a package-global slice append of parsed tickets in loadErgs -> TestScalingHeapRetention red on check/list/ready (~12 MB retained vs 1 MiB slack).\n\nThis file has real fangs on both invariants. The two negative controls are self-contained self-tests (they manufacture their own resource leak) and pass at baseline while asserting the guard's predicate direction with shared helpers/constants -- recorded as equivalent (not accusations) with positive non-vacuity arguments. The read paths use os.ReadFile and git .Output()/.Run() (self-closing/reaped), so for a resource-hygiene guard the faithful defect class is an ADDED leak, which is exactly what both guards and their controls detect. No toothless/survived findings; no fixes needed.

## Next step (out of scope here)
Applying the suggested fangs is a follow-up ticket — this audit changes no test code. Spot-check 2–3 toothless rows against their distinguishing input before acting.